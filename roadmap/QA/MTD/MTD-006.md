# MTD-006 — Security Headers e Rate Limiting em Produção

## Flow
[FLOW-007](../Flow/FLOW-007.md) — Segurança HTTP e Headers

## Objetivo
Validar que todos os security headers HTTP estão configurados corretamente em produção, que endpoints de desenvolvimento estão bloqueados, e que o rate limiting atua nos endpoints críticos de autenticação.

## Pré-condições
- Acesso à URL do ambiente de produção ou staging
- Browser com DevTools (Chrome recomendado)
- Ferramenta de linha de comando: `curl` disponível
- Acesso ao site `https://securityheaders.com` (verificação de headers)

---

## Cenário 1 — Verificação de security headers via DevTools

### Passos
1. Abrir o browser e acessar o frontend da aplicação
2. Abrir DevTools → aba **Network**
3. Recarregar a página (F5)
4. Selecionar qualquer request para a API backend (ex: `/items` ou `/auth/me`)
5. Abrir a aba **Headers** da response
6. Verificar cada header listado na tabela abaixo

### Resultado Esperado

| Header | Valor esperado |
|--------|---------------|
| `X-Frame-Options` | `DENY` ou `SAMEORIGIN` |
| `X-Content-Type-Options` | `nosniff` |
| `Strict-Transport-Security` | Presente (ex: `max-age=31536000; includeSubDomains`) |
| `Content-Security-Policy` | Presente com diretivas básicas |
| `Referrer-Policy` | Presente |

### Evidência
- [ ] Screenshot dos response headers com todos os itens acima visíveis

---

## Cenário 2 — Auditoria via securityheaders.com

### Passos
1. Acessar `https://securityheaders.com`
2. Inserir a URL do backend de produção (ou do frontend)
3. Executar a verificação
4. Anotar a nota obtida (A, B, C, D, E, F)

### Resultado Esperado
- Nota mínima **B** (ideal: A ou A+)
- Sem itens com ícone vermelho de crítico

### Evidência
- [ ] Screenshot do relatório do securityheaders.com com a nota

---

## Cenário 3 — Swagger e documentação bloqueados em produção

### Passos
1. Acessar no browser (em produção):
   - `https://[url-api]/swagger-ui.html`
   - `https://[url-api]/swagger-ui/index.html`
   - `https://[url-api]/v3/api-docs`
   - `https://[url-api]/v2/api-docs`
2. Para cada URL, verificar o código HTTP retornado

### Resultado Esperado
- Todas as URLs retornam **HTTP 404** (ou 403)
- Nenhuma documentação de API acessível publicamente

### Evidência
- [ ] Screenshot de cada URL com 404 (ou 403)

---

## Cenário 4 — Actuator bloqueado em produção

### Passos
Acessar os seguintes endpoints via browser ou curl:
```bash
curl -v https://[url-api]/actuator/env
curl -v https://[url-api]/actuator/beans
curl -v https://[url-api]/actuator/mappings
curl -v https://[url-api]/actuator/health
```

### Resultado Esperado
- `/actuator/env`, `/actuator/beans`, `/actuator/mappings` → **403 ou 404**
- `/actuator/health` → **200** mas com body simplificado: `{"status":"UP"}` **sem detalhes de infraestrutura**

### Evidência
- [ ] Output do curl para cada endpoint

---

## Cenário 5 — Rate limiting no endpoint de login

### Passos
Executar o script abaixo (ou equivalente manual com Postman/DevTools):
```bash
for i in $(seq 1 12); do
  STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X POST https://[url-api]/auth/login \
    -H "Content-Type: application/json" \
    -d '{"email":"test@test.com","password":"senhaerrada"}')
  echo "Tentativa $i: HTTP $STATUS"
done
```

### Resultado Esperado
- Tentativas 1–10: HTTP **401** (credenciais inválidas)
- Tentativas 11+: HTTP **429** com header `Retry-After`

Verificar o header `Retry-After`:
```bash
curl -v -X POST https://[url-api]/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"senhaerrada"}'
# (após 10 tentativas)
```

### Evidência
- [ ] Output do script mostrando transição de 401 para 429
- [ ] Header `Retry-After` visível na resposta 429

---

## Cenário 6 — Rate limiting no reset de senha

### Passos
Enviar 4 requisições de reset para o mesmo e-mail em menos de 15 minutos:
```bash
for i in $(seq 1 4); do
  STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X POST https://[url-api]/auth/password-reset/request \
    -H "Content-Type: application/json" \
    -d '{"email":"usuario-teste@example.com"}')
  echo "Tentativa $i: HTTP $STATUS"
done
```

### Resultado Esperado
- Tentativas 1–3: HTTP **200** (ou 204)
- Tentativa 4+: HTTP **429** com `Retry-After`

### Evidência
- [ ] Output do script mostrando 429 na 4ª tentativa

---

## Cenário 7 — CORS bloqueando origem não autorizada

### Passos
```bash
curl -v -H "Origin: https://site-malicioso.com.br" \
  -H "Access-Control-Request-Method: GET" \
  -X OPTIONS https://[url-api]/items
```

### Resultado Esperado
- Response **sem** header `Access-Control-Allow-Origin`
- Ou `Access-Control-Allow-Origin` **não inclui** `site-malicioso.com.br`

### Evidência
- [ ] Output do curl sem `Access-Control-Allow-Origin` para origem não autorizada

### Notas / Riscos
- TASK-010 (secrets auditados) — verificar também que nenhuma chave de API está no histórico git antes do launch
- Rate limiting baseado em IP — testar de IPs diferentes confirma que bloqueio é por IP, não por sessão

## Tags
`segurança` `headers` `rate-limiting` `CORS` `actuator` `swagger` `launch-critical`
